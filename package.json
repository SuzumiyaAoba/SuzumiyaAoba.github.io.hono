{
  "scripts": {
    "prepare": "husky",
    "format": "bunx @biomejs/biome check --apply .",
    "lint": "bunx @biomejs/biome check ."
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "husky": "^9.0.10",
    "lint-staged": "^15.2.2",
    "typescript": "^5.3.3"
  },
  "lint-staged": {
    "*.ts": "bun run lint"
  }
}
