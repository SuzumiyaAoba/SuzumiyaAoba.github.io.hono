{
  "main": "./src/index.tsx",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "dev": "vite",
    "preview": "vite preview",
    "build": "bun run ./build.ts --mode production",
    "format": "bunx @biomejs/biome check --apply .",
    "lint": "bunx @biomejs/biome check ."
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@hono/vite-dev-server": "^0.5.0",
    "@hono/vite-ssg": "^0.0.1",
    "husky": "^9.0.10",
    "lint-staged": "^15.2.2",
    "typescript": "^5.3.3",
    "vite": "^5.1.1",
    "vite-tsconfig-paths": "^4.3.1"
  },
  "lint-staged": {
    "*.ts": "bun run lint"
  }
}
